{
  "manifest_version": 3,
  "name": "Cloak File Picker",
  "version": "1.0.0",
  "description": "Replaces the OS file picker with a sandboxed list of files in ~/uploads/. Prevents the streamed desktop from being exposed to the end user.",
  "host_permissions": [
    "http://127.0.0.1/*"
  ],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_start",
      "all_frames": true
    }
  ]
}
